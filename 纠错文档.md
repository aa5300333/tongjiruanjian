# 纠错文档 (Correction Document)

本文档记录了系统中常见的错别字及其对应的正确文字。解析引擎在处理输入前会先参考此文档进行自动转换。

- [2026-05-14] 修复“01 234尾”等模式下 01 被错误展开为尾数的问题
    - **问题现象**: `expandHeadTail` 在某些情况下将 `01` 识别为可展开的簇（尤其是紧贴后缀时），导致 `01 234尾` 被错误识别。
    - **修复方案**: 强化 `expandHeadTail` 的 `shouldExpand` 判定。对于 `length > 1` 的数字块，若以 `0` 开头（即补零球号），则强制禁止其作为簇执行展开，无论其是否紧贴后缀。
    - **AI提示词**: `expandHeadTail` 中对于 `lastDigitIdx` 的展开必须排除以 `0` 开头的双位数（如 `01-09`），确保其作为独立球号被识别而非误触尾数展开逻辑。

- [2026-05-14] 修复“头/尾/范围/分类”在并集模式（空格分隔）下被忽略的问题
    - **问题现象**: 引入空格敏感的 `wordSegments` 逻辑后，原有的全局正则提取逻辑（head/tail/range）产出的结果未被正确合并到最终的分段对象中，导致“9尾 10各20”只能识别到 10。
    - **修复方案**: 将“头/尾/范围/合数/分类”的探测逻辑全面移入 `wordSegments` 循环内部，确保每个单词均经过动态模式扫描，并在交集（intersection）和并集（union）两条路径中均提供支持。
    - **AI提示词**: 在 `parseSegment` 的单词循环中，必须优先执行动态模式（Head, Tail, Range, Composite, Category）的匹配，并将结果同步注入 `intersectionFilters`（用于交集检测）和 `localTokens`（用于并集并保留顺序）。

- [2026-05-14] 规范波色显示逻辑
    - **问题现象**: 用户输入“红双”等交集组合时，预览中“红”字后的“波”后缀失踪。
    - **修复方案**: 在 `parseSegment` 最后拼装 `raw` 时，对 `rawTokens` 进行检查，若存在孤立的“红/蓝/绿”关键词，强制补齐为“红波/蓝波/绿波”。
    - **AI提示词**: 规范化显示：在处理并集或交集目标时，针对单一颜色词强制补齐“波”后缀。

- [2026-05-14] 增强交集/并集空格敏感识别及波色自动展开
    - **问题现象**: 
        1. 之前逻辑对交集的判定在空格处理上不一致。用户要求：**空格是核心分界线**：有空格为并集（如“红双 小”表示“红双”和“小”各多少），无空格为交集（如“红双小”表示满足三种属性的号码）。
        2. 复合波色（如“蓝绿波”）识别后显示不完整，且丢失了“波”字后缀（变为“蓝 绿”）。
    - **修复方案**: 
        1. 重构 `parseSegment`：首先按空格将片段切分为单词。对**每个单词内部**执行交集探测；如果单词内部有多个属性且交集有效，则该单词视为一个交集下注。
        2. 单词与单词之间、单词与独立数字/生肖之间统一视为并集，预览时强制使用空格分隔（`rawTokens.join(' ')`）。
        3. 引入 `expandWaves` 预处理函数：将“蓝绿波”等模式识别并替换为“蓝波 绿波”，确保下游逻辑能正确提取到完整的“颜色+波”属性。
    - **AI提示词**: 
        1. **规范颜色显示**：在 `parseSegment` 的最后拼接阶段，对 `rawTokens` 进行检查，若存在孤立的“红”、“蓝”、“绿”，必须统一追加“波”后缀。
        2. **禁用波字剥离**：在 `cleanDisplayRaw` 中不再强制剥离“波”字。
        3. **并集不计算去重**：针对并集场景（空格分隔），号码总数应为各组数量之和，不执行 Set 去重。
11: 
12: - [2026-05-13] 修复风险分析排名未跟随“风险容忍限额 (eatingThreshold)”随动的问题
    - **问题现象**: 风险分析排行榜（RiskAnalysisRanking）在“统计”和“吃码”页面中，其红色高亮背景仅基于 `risk < 0` 进行硬编码判断，未考虑用户设定的风险容忍度。
    - **修复方案**: 为 `RiskAnalysisRanking` 组件增加 `threshold` 属性，并将其逻辑修改为 `risk < -threshold`。同时同步更新所有调用处，确保全局风险预警与用户设置一致。
    - **AI提示词**: 风险分析高亮必须动态引用 `eatingThreshold` 状态。对比逻辑为 `risk < -threshold`，不得硬编码为 0。

- [2026-05-13] 增强组合类型识别 (大单/大双/小单/小双) 及 "包" 金额平摊逻辑
    - **问题现象**: 1. 用户输入“大单”时，系统将其拆分为“大”和“单”的并集，而非交集；2. 用户需要一种方式平摊总金额到多个号码上（如“龙包100”）；3. “小数双”等变体未能正确识别为“小双”。
    - **修复方案**: 
        1. 在 `combinations` 数组中显式增加 “大单”、“大双”、“小单”、“小双” 以及 “合单单”、“合双双”、“合大大”、“合小小” 组合，确保其在单项词（如“大”、“单”）之前被优先匹配并执行交集过滤。
        2. 将 “包” 加入 `STRONG_KEYWORDS`，并在 `parseSegment` 逻辑中识别该关键字。若匹配到 “包”，则将提取到的金额除以目标号码总数，实现平摊。
        3. 在 `parseSegment` 和 `cleanDisplayRaw` 中增加“大数/小数/单数/双数”到“大/小/单/双”的归一化，并修正交叉组合（intersection）逻辑，使其基于归一化后的字符串进行匹配。
    - **AI提示词**: “大单”等组合词必须在 `combinations` 中显式定义为交集过滤器。新增 “包” 关键字逻辑。在提取逻辑中，优先执行“大数”等词汇的归一化，且交叉识别（Intersection Filters）必须基于归一化后的 `remainingStr`。

- [2026-05-12] 修复“清空数据并粘贴”按钮失效及跨窗口/Electron同步问题
    - **问题现象**: 1. 点击“清空数据并粘贴”后，输入框未更新内容或未触发表单重置；2. 在独立录入窗口下，清空信号无法同步到主窗口；3. 粘贴后的文本未同步更新 `localModalValue` 导致预览区域显示滞后。
    - **修复方案**: 
        1. 在 `triggerClearAndPaste` 中显式读取剪贴板并发起 `handleReset` 同步。
        2. 引入 `LOTTERY_RESET_REQUEST` 全局信号（以及 Electron 的 `reset-entry`），确保跨进程数据清空。
        3. 强制同步更新 `modalInputValue` 和 `localModalValue`。
        4. 将 `triggerClearAndPaste` 及其依赖函数接入 `useMemo` 的依赖项，确保子组件引用的是最新闭包。
    - **AI提示词**: “清空数据并粘贴”必须同时更新 `modalInputValue` 和 `localModalValue`。在 `standaloneMode` 下必须通过 `localStorage` 或 `electron.send` 发送全局重置信号，并通过 `handleReset` 即时清理本地内存。

- [2026-05-12] 扩展关键词归一化 (个 -> 各)
    - **问题现象**: 用户习惯使用“个”作为金额赋值符（如“10个20”），系统有时无法稳定识别为“各”。
    - **修复方案**: 在 `TYPO_MAP` 中将 `'个'` 映射为 `'各'`。
    - **AI提示词**: 将金额语义词 `'个'` 加入解析前的 `TYPO_MAP` 归一化列表，确保其逻辑与 `各` 保持一致。

- [2026-05-09] 修正尾数展开抑制逻辑。解决“14257尾”在带点号列表环境下被错误抑制并剥离的问题。
    - **问题现象**: 输入 `11. 14257尾` 时，解析器将 `14257` 视为金额或普通号码，而非尾数展开。
    - **修复方案**: 在 `expandHeadTail` 中加入 `val <= 49` 判定。只有当疑似尾数的数字属于合法球号范围时，才在列表环境中执行抑制逻辑。
    - **AI提示词**: 修改 `expandHeadTail` 中的 `inhibitExpansion` 判定，增加 `val <= 49` 条件。

- [2026-05-11] 修正汇总页面缩放逻辑失效及显示不一致
    - **问题现象**: 1. 汇总页面的号码分布矩阵错误显示了原始总额而非按系数缩放后的金额；2. 吃码上报解算器及风险排名在汇总模式下未考虑各客户的算式系数。
    - **修复方案**: 
        1. 引入全局 `displayBetData` memo。当 `selectedCustomerId === 'default'` 时，强制返回 `scaledBetData`。
        2. 将 `totalTurnover`、`NumberMatrix`、`previewReportedData` (解算器) 以及 `riskDisplayData` 统一重构为使用 `displayBetData`。
    - **AI提示词**: 汇总页面（Summary Mode）的所有视图组件和逻辑计算必须统一依赖于 `displayBetData` (即 `scaledBetData` 的别名)，确保系数缩放逻辑贯穿全站。

- [2026-05-11] 录入弹窗客户状态持久化优化
    - **问题现象**: 录入弹窗再次打开时，默认总是选中第一个客户，用户希望保持上次录入时的选择状态。
    - **修复方案**: 
        1. 在 `popOutTargetId` 的初始化逻辑中加入从 `localStorage` 读取 `last_recorded_customer_id` 的判定。
        2. 在 `handleParse` 成功保存数据后，将 `popOutTargetId` 物理记入 `last_recorded_customer_id`。
    - **AI提示词**: 实现 `popOutTargetId` 时，必须结合 `localStorage` 记录 `last_recorded_customer_id`。保存下注时同步更新该 ID，确保弹窗再次打开时能恢复上一次的上下文。

- [2026-05-11] 吃码上报页面 UI 逻辑重构 (双框显示)
    - **问题现象**: 用户希望更直观地看到汇总额减去上报额的过程，并增加“汇总数”和“余下上报”的标题。
    - **修复方案**: 
        1. 在吃码矩阵上方增加每列对应的“汇总数”与“余下上报”标题。
        2. 将单一数值框拆分为双框：左框显示 `总额 - 已上报` (实地自留)，右框显示 `本次建议上报差额`。
        3. 利用 React 自动响应 state 变化的特性，使执行上报后数值自动从右框“流动”到左框的扣减逻辑中。
    - **AI提示词**: 吃码上报页面必须采用双框布局（汇总数 vs 余下上报），且左框始终展示扣除已上报后的实地余额。确保执行操作后，界面能实时反映“金额从右框转移并导致左框进一步扣减”的业务流向。

- [2026-05-11] 智能系统识别开关与默认系统调整
    - **问题现象**: 用户希望可以关闭自动识别“港/澳”地区词的功能，关闭后所有不带地区词的录入应强制归属澳门系统；同时“精准撤销”应默认关闭。
    - **修复方案**: 
        1. 在设置中新增 `smartSystemRecognition` 开关（默认 ON）。
        2. 将 `enableSearchUndo` 的默认初始值改为 `false`。
        3. 修改 `splitBySystem`：当 `smartSystemRecognition` 为 OFF 时，忽略所有关键词，直接返回单一的澳门（MO）下注片段。
    - **AI提示词**: 系统设置新增 `smartSystemRecognition` 开关。`splitBySystem` 必须首选校验此标志，若为 OFF 则强制返回 `[{ system: 'MO', text: input }]` 且不进行关键词分割。精准撤销默认关闭。

- [2026-05-11] 修正港澳特码系统识别与中奖判定错位
    - **问题现象**: 1. 香港系统的下注在结算时错误地使用了澳门特码框的数值进行判定（反之亦然），导致“指东打西”的中奖错误。
    - **修复方案**: 
        1. 修正历史流水显示（sidebar/modal）以及报表导出中的中奖判定逻辑，显式通过 `item.system`（若有）或 `record.system` 结合系统前缀来动态选择对应的特码框数值（`auxSpecialNumber` vs `specialNumber`）。
    - **AI提示词**: 所有涉及中奖判定的代码必须基于 `item.system` 动态选择 `hkSpecial` (aux) 或 `moSpecial` (main) 框的值，严禁硬编码单一系统判断。无引导词录入默认归属 MO。

- [2026-05-11] 报表批注框自适应尺寸优化
    - **问题现象**: 导出的 Excel 报表中，下注金额列的批注（Annotations）框尺寸固定且往往过小，导致长文本原数据被遮挡。
    - **修复方案**: 在导出逻辑中引入 `wpx` 和 `hpx` 计算逻辑。根据 `originalText` 的行数以及最长行的字符跨度（区分中英文权重），动态设置批注框的宽（wpx）和高（hpx）。
    - **AI提示词**: 在 Excel 导出时，通过 `d.originalText` 动态计算 comment 对象的 `p: { wpx, hpx }` 属性，确保批注内容完整显示。

- [2026-05-11] 解决清空面板持久化失效及跨系统不同步问题
    - **问题现象**: 1. 清空面板后切换系统会还原数据；2. 清空某一系统（如香港）的面板时，同一客户在另一系统（如澳门）的数据依然保留，未达到“一键清空该客户”的效果。
    - **修复方案**: 
        1. 在 `handleClearBoard` 中显式循环 `['', 'MO_']` 前缀。
        2. 同步执行 `localStorage.setItem` 对两个系统的该客户状态（`customer_state_${id}`）进行物理覆盖。
    - **AI提示词**: “清空面板”逻辑必须包含同步的 `localStorage.setItem` 物理写入，且必须**遍历所有前缀 ('' 和 'MO_')** 来同时清除该客户在港、澳两个系统的业务数据。

- [2026-05-10] 移除所有录入目标的强制去重
    - **问题现象**: 输入 `40 40各15` 或 `0*1*9*2*9尾各30` 时，解析器自动去重，导致重复的号码或尾数只录入了一次。
    - **修复方案**: 
        1. 移除 `normalizeComposites` 逻辑中的 `seenAggregates` 去重 Set，允许所有目标（号码、分类、头尾等）在片段内重复出现。
        2. 确保 `parseSegment` 返回结果前的 `numbers` 组装逻辑不执行 `Set` 去重，允许重复项透传。
    - **AI提示词**: 解析逻辑应尊重用户手动输入的重复意图，严禁对任何目标类别执行自动去重。在 `normalizeComposites` 和 `parseSegment` 结尾处均不得使用 `Set` 或其它逻辑过滤重复项。

- [2026-05-10] 扩展金额关键词归一化
    - **问题现象**: 类似 `一个号`、`各号` 等词汇在某些复杂输入下无法被识别为金额赋值符。
    - **修复方案**: 将 `一个号`、`各号`、`每个号`、`个号` 加入 `TYPO_MAP` 统一归一化为 `各`。
    - **AI提示词**: 将金额语义词（如 `一个号`、`各号` 等）加入解析前的 `TYPO_MAP` 归一化列表，确保其逻辑与 `各` 保持一致。

- ## 颜色类
- 兰 -> 蓝
- 绿 -> 绿 (注：防止部分输入法输出繁体或异体字)
- 红 -> 红

## 系统/地区类
- 门 -> 澳 (在特定上下文，但在系统识别中已有逻辑处理)
- 港码 -> 港
- 澳码 -> 澳

## 生肖类
- 猪 -> 猪
- 鼠 -> 鼠
- 虎 -> 虎

## 数字类
- 零 -> 0
- 一 -> 1
- 二/两 -> 2
- 三 -> 3
- 四 -> 4
- 五 -> 5
- 六 -> 6
- 七 -> 7
- 八 -> 8
- 九 -> 9
- 十 -> 10

## 逻辑类
- 一字 -> 各
- 字 -> 各
- 个字 -> 各
- 个 -> 各
- 一粒 -> 各
- 俩 -> 2

## 逻辑追溯记录
- [2026-05-10] 合单/双/大/小 识别逻辑修正 (深度优化：动态交集 & 紧凑显示)
    - **问题现象**: 1. 硬编码复合词导致识别逻辑判定失效；2. `cleanDisplayRaw` 曾因 `complexRegex` 不完善而向 `合单单` 等复合词注入空格，导致其被误识别为并集而非交集。
    - **修复方案**: 
        1. 依赖 `intersectionFilters` 动态拆分识别。
        2. 增强 `complexRegex` 支持 `[大小单双]+`，并在 `normalizeComposites` 中有条件地保留“合”字（仅在紧跟数字时补“数”）。
        3. 彻底修复 `cleanDisplayRaw` 逻辑，确保 `合单单` 等在预览中保持紧密不分离 (raw: "合单单")，从而触发正确的无分隔符逻辑。
    - **AI提示词**: 实现复合属性识别时，必须确保 `cleanDisplayRaw` 不破坏输入词的紧凑性。复合正则匹配必须支持贪婪匹配 `[特性词]+`。

- [2026-05-10] 合数识别与显示优化 (二次修正)
    - **问题现象**: `合数11` 被错误显示为 `合数1 01`，且某些情况下出现 `合数12 合数 12` 这种重复显示。
    - **修复方案**: 
        1. 引入统一的 `normalizeComposites` 函数，在解析（parseSegment）和预览（cleanDisplayRaw）前执行。
        2. 确保合数及其数字在 `targetsStr` 中被完全归一化并通过 `complexRegex` 实施保护，防止被 `cleanRawText` 错误分割。
        3. 移除 `cleanDisplayRaw` 中相互重叠的逻辑，确保 expansion 逻辑幂等。
    - **AI提示词**: 处理合数显示和解析时，必须在提取独立数字前调用统一的 `normalizeComposites`。该函数应确保“合X”与“X合”均被规整为“合数X”，防止 `cleanRawText` 将多位数（如11）拆分。

- [2026-05-10] 合数识别逻辑修正
    - **问题现象**: `合数10` 等多位数被拆分，且对 `合10` 的定义不明确（应为绝对和）。
    - **修复方案**: 
        1. 修改 `cleanDisplayRaw` 中的 regex，使用 `\d+` 匹配数字块，保留 `10`, `11` 等整体，并确保预览中不带空格。
        2. 合数计算逻辑优化：如果值 `val >= 10`，执行绝对和匹配 `(tens + ones) === val`；如果 `val < 10`，执行尾数和匹配 `(tens + ones) % 10 === val`。
        3. 解析后对号码集合执行 `Set` 去重。
    - **AI提示词**: 修改合数解析逻辑时，必须使用 `\d+` 匹配数字块。计算时：值 >= 10 则匹配 `(tens + ones) === val`，值 < 10 则匹配 `(tens + ones) % 10 === val`。

- [2026-05-10] 增加“俩”字映射。
    - **问题现象**: 用户习惯直呼“俩”表示数字 2（如“俩尾”）。
    - **修复方案**: 在 `TYPO_MAP` 中将 `'俩'` 映射为 `'2'`。
    - **AI提示词**: 语义归一化应包含 `'俩': '2'`。

- [2026-05-10] 修复录入助手撤销/清空功能失效。
    - **问题现象**: 独立窗口模式下点击“撤销”或“清空并粘贴”无反应。
    - **修复方案**: 统一跨窗口信号键名。发送端在 `localStorage.setItem` 时增加 `getSysKey` 前缀，使其与接收端保持一致。
    - **AI提示词**: 任何跨窗口通过 `localStorage` 传递的控制信号（如 `UNDO`、`RESET`）必须通过 `getSysKey` 包装，以适配港澳系统隔离。

- [2026-05-10] 修正 `6尾各50. 3. 9尾` 场景下的识别失败。
    - **问题现象**: `.` 紧跟在金额 `50` 后时，导致 `expandHeadTail` 将其连同后面的 `3` 错误捕获，且由于 `50` 存在，触发了“多位数抑制”，导致 `3` 无法展开为 `3尾`。
    - **修复方案**: 
        1. 在 `preProcessed` 中将 `金额. `（点号加空格）规范化为 `金额 `。
        2. 修正 `expandHeadTail` 的抑制逻辑，明确规定只有 `1-49` 之间的数字才具备抑制周边号码展开的能力。
    - **AI提示词**: 任何 > 49 的数字都不应被 `expandHeadTail` 的 `inhibitExpansion` 逻辑识别为“多位球号干扰项”。

- [2026-05-10] 修复跨行头尾展开干扰及非法 00 显示。
    - **问题现象**: 输入多行数据时，第二行的单数字（如 1.0.5...）未能按“尾数”展开，反而显示为 `00`。
    - **修复方案**: 
        1. 在 `expandHeadTail` 正则中将 `\s` 替换为 `[ \t]` 以物理隔离换行符。
        2. 在 `cleanDisplayRaw` 和 `smartSplitDigits` 中增加范围校验，拦截独立数字 `0`。
    - **AI提示词**: `expandHeadTail` 的匹配缓冲区严禁包含 `\n`。在 `cleanDisplayRaw` 格式化预览时，必须校验 `val >= 1` 才执行补零。

- [2026-05-10] 修正流水记录展示顺序。
    - **问题现象**: 入账流水的片段顺序与识别助手中的顺序不一致（受金额排序影响）。
    - **修复方案**: 移除流水生成时的金额排序逻辑，改用 `items.map` 直接按识别顺序生成预览。
    - **AI提示词**: 保持 `segmentPreview` 的生成逻辑与 `items` 的原始顺序一致，严禁引入 `sort` 或基于金额的 `Record` 聚合。

- [2026-05-10] 强化“一字认做各”逻辑。
    - **问题现象**: 用户习惯使用“一字”或单纯的“字”作为金额赋值符，若未显式处理可能导致解析失败或显示混乱。
    - **修复方案**: 在 `TYPO_MAP` 中加入全局映射，将相关词汇统一归正为“各”。
    - **AI提示词**: 在 `correctTypos` 或 `TYPO_MAP` 中加入 `'一字': '各'`, `'字': '各'` 等映射，实现语义归一化。

- [2026-05-09] 建立纠错机制。解决“兰”等错别字导致颜色识别失败的问题。
    - **问题现象**: 用户输入“兰波”或“兰尾”，系统无法识别颜色。
    - **修复方案**: 在解析前执行全局字符替换。
    - **AI提示词**: 在解析逻辑开始前，调用 `correctTypos` 函数执行预定义的正则替换。
