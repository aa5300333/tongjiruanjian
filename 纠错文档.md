# 纠错文档 (Correction Document)

本文档记录了系统中常见的错别字及其对应的正确文字。解析引擎在处理输入前会先参考此文档进行自动转换。

- [2026-05-20] 修复大盘汇总模式下平摊玩法中金彩金偏高、对账多算四舍五入偏离的 regression Bug
    - **问题现象**: 
        当用户录入“龙包10”等平摊玩法时，4 个号码被分摊成为各 2.5 元（由于矩阵中限制整数，矩阵真正入账为 2 元）。当中特码时，流水列表中显示的中金应该为 `Math.floor(1 * 2.5) = 2` 元。但如果在汇总视图（大盘显示）下，用户发现中金竟然显示了 3 元！
    - **修复方案**: 
        1. **分析根源**: 汇总模式由于需要将所有子客户的数据合并，并乘上各个子客户对应的算式系数（`coeffKey = "coefficient_${c.id}"`）。在进行系数放大的映射处理中，系统在 1706 行和 1722 行对单个 item 的 amount 转换使用了强 `Math.round((it.amount || 0) * coeff)`。当 coeff = 1.0 时，把 2.5 强转为了 3，直接破坏了原始数据精确度，使得下游即使使用 `Math.floor` 取整，也是基于已经被四舍五入到 3 的金额进行的，从而引发不对齐的偏差。
        2. **保护平摊玩法的浮点数转换**: 修改 `App.tsx` 中的合并汇总 mapping 函数。如果当前项目 `it.isSplitAmount` 为真，则乘以系数后保持浮点数数值比例（即 `(it.amount || 0) * coeff`），只有对普通的、无需平摊的常规下注，才继续使用 `Math.round` 保持整数转换。这使得下游的 `Math.floor(hits * item.amount)` 在汇总模式下能重新基于浮点金额精确计算，不产生四舍五入偏离，获得彻底完美咬合的对账派彩。
    - **AI提示词**: 在汇总大盘客户记录合并（Coefficient 转换）的 `map` 中，针对 `financeRecords` 和 `compoundRecords` 的 `items` 循环，更新 `amount` 时必须判断 `it.isSplitAmount`。如果为真，则一律乘以 `coeff` 保持实值不作 `Math.round` 前置取整，以防止平摊浮点数（例如2.5）被强制偏大进位，保障下游中金计算和派彩在各种账单视图和导出状态下的精密咬合及对称还原。

- [2026-05-20] 开启大盘/汇总页面“清空面板”按钮及跨客户全量清空数据的深度支持
    - **问题现象**: 
        系统的“清空面板”按钮在切换为“大盘/汇总客户”（即 `selectedCustomerId === 'default'`，全部客户汇总状态下）时被物理禁用。如果用户想重置所有录入的数据开始新一轮的记账运算，必须跳转到每一个客户各自分别清空、或选择“软件业务数据全清零”这样会重新刷新并重建界面的强力手段，缺少一键干净清零双系统各客户业务数据的按钮。
    - **修复方案**: 
        1. **解除按钮禁用约束**: 将主视图中“清空面板”按钮的禁用条件由原本的 `disabled={selectedCustomerId === 'default'}` 强力剥除，允许大盘汇总状态下直接点击。
        2. **构建无损对称全局清理**: 
           在 `handleClearBoard` 中截获 `selectedCustomerId === 'default'` 的全局请求类型。当用户点击并确认后：
           - 在内存中强制重置当前的 `financeBetData` 状态机与流水等关键数据，使其彻底干净重组归零。
           - 物理同步执行 `localStorage.setItem`，将不带前缀的大盘/公共顶级键（如 `MO_financeBetData` 等）彻底清空覆盖。
           - 跑一遍客户循环（排除 `default`），将 `localStorage` 中所有真实生效客户的 `customer_state_`、`MO_customer_state_` 以及 `HK_customer_state_` 业务内容物理降级清空，从而实现对全系统无空隙的完美对齐。
           - 通过递增 `refreshCounter`，迫使整盘流水重新合并和渲染，保持系统高配置完整度的同时优雅实现无死角一键清理。
    - **AI提示词**: 若要支持汇总页面一键物理及内存清空：须解除 `clearBoard` 按钮对应 `selectedCustomerId === 'default'` 的禁用 and 中途拦截。清空时不仅在内存 states 执行归零，还应循环清理所有真实客户的 `customer_state_` 列物理缓存并清除公共大盘键。由于非 atomic 操作，清理完毕必须执行 `setRefreshCounter` 以刷新大盘视图。

- [2026-05-20] 单特定客户及汇总大盘跨港澳系统合计、两地总和与号码分布矩阵球图对账不一致的缺陷
    - **问题现象**:
        当选中特定客户（如“张三”）并启用折算系数（如0.70）时，虽然大盘/汇总页面采用系数后展示各球（5元 * 0.70 = 3.5 进位为 4 元，10个号共40元），但在该客户详情页中，号码分布矩阵显示的是未经折算的原始数据（即一球 5 元，总共 50 元）。此时若外加：
        1. 在汇总/大盘页为了方便同已被折算的矩阵直接对账，合计应该按各球乘以系数四舍五入分别取整后再相加。
        2. 在客户详情页为了同原本未折算的矩阵直接对账，合计应该按各球原本的原始未折算金额相加。
    - **修复方案**:
        1. **判定视图进行不同底层逻辑统计**: 
           - **客户详情页模式**（`selectedCustomerId !== 'default'`）下，`macauTotal` 与 `hkTotal` 直接累加客户页对应系统 1~49 里的原始数值（不受系数影向），得到原始面额的总数大计，与客户自身的矩阵数相契合。
           - **汇总/大盘页模式**（`selectedCustomerId === 'default'`）下，统计计算则依然使用**各球金额乘以各自客户系数后四舍五入取整再求和**的最新完美对账计算法（“球级系数对账加总法”），使其天然与汇总矩阵数据严格一致。
        2. **实现效果**: 录入、汇总、折算对账三级完全融会贯通，不管在哪个详情视图下，底下合计值和头顶上矩阵中看到的每一个球的值相加都是绝对咬合的。
    - **AI提示词**: 区分对待各级统计合计：在客户详情页，计算合计时必须返回其原始未乘以系数折算的原本号码金额总和；在汇总模式下，则必须先遍历该客或所有真实子客户，对该系统下的 1 至 49 每一个号码球独立乘以对应的 `coefficient_${customerId}` 折算系数后使用 `Math.round` 取整，再将 49 个号码取整额进行二次大加总，从而实现汇总矩阵展示额与底部总计在任何算式折算模式下的绝对完美对账。

- [2026-05-20] 修复大模型 (Gemini OCR) 识别图片报错 API 请求 payload 结构不兼容的 Bug
    - **问题现象**:
        在使用 Gemini 大模型识别图片时，控制台或界面弹窗报错（例如 `parts is required on Content` 等对象校验异常或格式错误），导致大模型识别流程完全中断，且历史默认模型 fallback 值 `'gemini-3.5-flash'` 在设置下拉选项中缺失造成状态不锁合。
    - **修复方案**:
        1. **规范 contents 载荷结构**: 将 `src/utils/geminiOcr.ts` 中传入 `ai.models.generateContent` 的 `contents` 参数，由直接 Part 数组 `[ { inlineData }, { text } ]`，修改为符合最新 `@google/genai` SDK 规范的 `{ parts: [ { inlineData }, { text } ] }` 单一 Content 对象承载。
        2. **对齐模型默认设定与下拉列表**: 将默认的模型设定由 `'gemini-3.5-flash'` 全面统合更改为 `'gemini-2.5-flash'`，并在 `src/App.tsx` 中的主设置、独立弹出窗设置以及 `performGeminiOcr` 的默认方法参数中全面对齐锁定，保证状态机逻辑锁死一致。
    - **AI提示词**: 使用 `@google/genai` SDK 时，多模态（包含图片和文本）的内容生成请求必须严格使用 `contents: { parts: [ { inlineData }, { text } ] }` 的格式。不得将 Parts 数组作为 `contents` 的直接数组元素传递，以防止最新版 SDK 校验抛出 `parts is required on Content` 结构错误。同时，默认 model 与设置下拉菜单中的 model 必须在全系统各组件状态里严格同步为 `gemini-2.5-flash`。

- [2026-05-20] 引入大模型 API 智能识别 (Gemini OCR) 作为离线 PaddleOCR 的智能备用引擎
    - **问题现象**: 
        离线本地 OCR 虽然快速且 100% 隔离，但在部分特殊手写体、微信低清模糊图片或倾斜角度很大的情况下，解析精度受阻，用户希望可以配备独立的大模型 API 作为高精度备用方案，并能自行管理 API Key 和选择型号。
    - **修复方案**: 
        1. **引入大模型客户端服务**: 建立独立的 `src/utils/geminiOcr.ts`，基于最新的 `@google/genai` 交互规范构建 OCR 解析机，向大模型注入定制的专业彩票玩法结构化提示词，使多模态大模型能在极恶劣墨迹或手写笔迹下自愈翻译并整理出标准下注大单。
        2. **两套设置菜单深度覆盖**: 分别在“主界面系统设置 modal”和“挂件/独立弹窗”中引入识别引擎选择块（PaddleOCR / Gemini），支持临时状态（temp）向永久状态（useState/localStorage）保存的高速流动。
        3. **前置深度业务清洗洗礼**: 在两路识别引擎返回内容的第一个微秒，一律将其强制传导进 `finalCleanText` 等核心纠错翻译字典运行一遍，达成大模型产物、PaddleOCR 产物在下游解析逻辑上的 100% 视觉对齐和对称兼容，不产生功能退化（Regression）。
    - **AI提示词**: 编写大模型 API 识别集成时，将 core API 逻辑收纳于 `src/utils/geminiOcr.ts`。UI 上同步支持本地 Paddle 模式、云端 Gemini 模式切换；大模型识别出的文本必须在第一时间输入 `finalCleanText` 进行净化纠偏再注入输入框。

- [2026-05-20] 修复录入窗口清空数据后输入框失去焦点、无法选中及无法输入的严重 Bug
    - **问题现象**: 
        在主窗口或独立窗口中，点击“清空并粘贴”按钮确认重置后，输入框（textarea）不仅失去焦点（闪烁光标剥落），还在特定平台/打包后的 Electron（Release 版本）中呈现无法被鼠标点中选中、键盘输入无任何反馈的“死锁不可编辑”状态。
    - **修复方案**: 
        1. **分析根源**: 历史上系统在 “清空数据/大段粘贴” 动作时，为了刷新视图通过触发 `setTextareaKey` 改变 React 的 `key`。这会导致 React 彻底物理销毁原输入框 DOM 节点，重新装载全新 DOM。而当点击 confirm 确定关闭窗口返回，由于页面大量重设事件和对 localStorage 的循环轰炸导致卡顿，销毁的瞬间使原本指向的 Ref 指向了非文档（detached）旧节点，新节点的 `focus()` 事件在生命周期不稳的情况下被系统吃掉，从而形成失焦死锁且无法选中。
        2. **物理保留 DOM**: 彻底废除在“清空数据/普通变更”时销毁重建整个输入框 DOM 的负优化逻辑（`setTextareaKey`）。
           在父组件 `triggerClearAndPaste` 与子组件 `EntryModalContent` 关于 `externalValue` 变化的 `useEffect` 中，严格约定只有对于字数极端多（如 `length > 1000`）的大文本粘贴才执行重挂载，而普通的清空（长度为 0 / Falsy）**坚决不改变 key、不销毁 DOM 节点**。这使输入框始终处于网页 Document 的主链路，Ref 引用始终物理持久稳定。
        3. **多阶段物理级抗干扰聚焦**: 运用不受 DOM 销毁影响的纯物理持久节点，在按钮点击后连续在 **立即同步、50ms、150ms、300ms、500ms** 时刻进行分级多次物理 `focus()` 和光标选区重定位，最大程度消解由于 confirm 对话框或剪贴板异步引发的系统窗口重新激活时的聚焦延迟与卡顿，完美实现一秒清空并 100% 毫无死角地秒重获光标的可编辑体验。
    - **AI提示词**: 清空数据（Clear & Paste）和外部输入赋空值时，绝对不要更改 `textareaKey` 重装输入框，保持 DOM 原样原生变空，并使用包含“立即、50ms、150ms、300ms、500ms”等多频节拍重试聚焦技术来牢牢锁定文本框焦点，防止因 confirm 退回或高负荷下的聚焦丢失与死锁。

- [2026-05-19] 修复 "包" 等平摊金额在入账和撤销 (handleUndo) 时因 Math.round() 导致对称计算偏差的 Bug
    - **问题现象**: 
        当用户录入 "蛇包10" 时，生肖蛇在 2026 年包含 4 个号码。系统自动平摊金额：10 / 4 = 2.5元。
        入账保存时，系统通过 `Math.round(prev + it.amount)` 存储数据。由于 `Math.round(0 + 2.5) = 3`，号码分布矩阵中各号码均存入了 `3` 元（总计 12 元）。
        当用户选择撤销删除该条流水时，撤销函数通过 `Math.round(prev - item.amount)` 执行扣减。由于矩阵中原值为 3 元，扣减式变为 `Math.round(3 - 2.5) = Math.round(0.5) = 1`，导致被删除后，矩阵中依然残留了 `1` 元无法归零。
    - **修复方案**: 
        1. 在 `src/App.tsx` 的入账保存逻辑中，不直接将增加后的结果取整，而是使用 `Math.round((prev + it.amount) * 100) / 100` 保留至多两位小数，从而支持完美的浮点存储：
           `data.financeBetData[num] = Math.round(((data.financeBetData[num] || 0) + it.amount) * 100) / 100;`
        2. 同样，在撤销 `handleUndo` 的扣减过程中，保持相同的双精度浮点数减法精度：
           `data.financeBetData[num] = Math.round(((data.financeBetData[num] || 0) - item.amount) * 100) / 100;`
        3. 这样，`2.5 - 2.5` 恰好等于 `0`，而显示端 `NumberMatrix` 渲染数字时本来就使用 `amount.toFixed(0)` 来对矩阵显示的值进行四舍五入展示（例如把 2.5 显示为 3，而归零后不显示），这既保证了前台展示依然是完美的整数 3，又实现了底层浮点数的 100% 精确扣减对齐，彻底解决了撤销后数字残留的数学 Bug。
    - **AI提示词**: 入账保存与 `handleUndo` 撤销动作修改 `financeBetData` 时，使用 `Math.round((val) * 100) / 100` 双精度保留两位小数加减，废除直接对结果进行 `Math.round` 取整导致的累加截断失真，显示端继续保持 `amount.toFixed(0)` 以满足整数球号的财务展示效果。

- [2026-05-19] 修复 "一个" 作为单位算符干扰金额解析并导致后方列表无法识别的 Bug
    - **问题现象**: 
        当输入形如 `"澳门特鼠各15一个，38.14.26.23.01.44各20"` 时，由于 `TYPO_MAP` 中没有直接提供 `"一个"` 这一多字算符的整体映射，而是由单字 `'个' -> '各'` 进行了分块。这导致 `"一个"` 变成 `"一各"`，进而被 `replaceChinese` 将 `"一"` 错误转换为 `"1"`，得到 `"151各"`.
        不仅令 `"15"` 的金额错误地识别为了 `"151"`，该处的 `"各"` 还在后续的 `splitByAnchors` 中被匹配为 `"各，38"` 的假锚点，导致后方 `"38.14.26..."` 等大长链下注目标彻底丢落。
    - **修复方案**: 
        1. 在 `src/utils/lotteryParser.ts` 的 `TYPO_MAP` 中，在单字 `'个'` 解析之上，加入较长优先级词组：`'各一个': '各'`, `'一个': '各'`, `'每个': '各'`，确保多字词被当作一整个标识符直接归一化到 `"各"`。
        2. 在 `parseInput` 核心解析引擎的 `replaceChinese` 过程后，新增冗余算符和物理单位拆弹清洗：
           `preProcessed = preProcessed.replace(/(各|字|包|名)\s*(\d+|\d+\.\d+)\s*(各|字|名|个|一个|个字|各一个)(?!\s*\d)/gi, '$1 $2 ');`
           当数字已具有 `"各/字/包/名"` 等前缀标识，其尾部紧跟的冗余标识算符将会被物理裁剪（安全过滤并避免其后无数字时被后方连号吸收），防止其作为独立假锚点引发下注丢单的连锁反应。
    - **AI提示词**: 在 `TYPO_MAP` 中将 `"一个"`, `"各一个"`, `"每个"` 整体映射归一化为 `"各"`。并在 `replaceChinese` 动作后加入修剪规则 `/(各|字|包|名)\s*(\d+|\d+\.\d+)\s*(各|字|名|个|一个|个字|各一个)(?!\s*\d)/gi` 替换成 `$1 $2 `，以彻底吞噬已配对数字后的假残留锚点阻断后车追尾。

- [2026-05-19] 修复生肖别字“𤠣”为“猴”，并完善“包”字平摊金额（四舍五入）警示
    - **问题现象**: 
        1. 用户在微信打字时，可能输入带有罕见异形字 “𤠣”（带有犬字旁的 `𤠣`），系统之前未加入纠错映射，导致带有此字生肖的指令丢落，未识别为“猴”。
        2. 关于平摊特征词“包”的金额平摊时（例如“蛇包10”平摊至5个号码，每只号码下注2.5元，四舍五入到最近的代数算式精度），用户希望能在预览界面的“合计”部分后面有直观的 “（四舍五入）” 提示文字，使业务对账更加透明易懂。
    - **修复方案**: 
        1. 在 `src/utils/lotteryParser.ts` 里面 `TYPO_MAP` 新增 `'𤠣': '猴'`，实现异形生肖字无缝翻译纠偏。
        2. 修改 `ParseResult` 接口类型，加入 `isSplitAmount?: boolean` 标志位。在 `parseSegment` 解析包字逻辑时设置该布尔值为 `true`。
        3. 在 `src/App.tsx` 中的保存账单解析预览 `segmentPreview` 以及动态打字同步渲染预览 `previewNodes` 逻辑中，当 `it.isSplitAmount` 标志位生效时，在合计金额后强制渲染提示后缀 `（四舍五入）`。
    - **AI提示词**: 支持生肖特定罕见异形字 `𤠣` 映射翻译为 `猴`。为平摊功能“包”在 `ParseResult` 返回中标记 `isSplitAmount: true` 并在系统提交预览与动态输入提示组件处，在合计文本尾追加 `（四舍五入）` 字符。

- [2026-05-19] 限制吃码上报的反馈范围：只入风险数，不入号码矩阵
- [2026-05-19] 修复金额截断造成的撤销功能数值多增缺陷，并高亮号码矩阵极端负值 (无需负数下注输入解析)

- [2026-05-20] 清理 Gemini-2.5-flash 与 Tesseract.js 图片识别，移出前端代码以准备本地 PaddleOCR-json 进程集成
    - **问题现象**:
        Web 端内嵌入包（如 Tesseract.js）字库文件过于庞大导致打包文件体积冗余，且云端 Google Gemini API 请求面临沙箱限制或昂贵流量费。按照用户明确指令，为了将精度拉高至物理完美的极限并且 100% 离线，需要将这两个高成本、繁琐且常有错漏的前端 OCR 移除，改为建议在桌面端打包好之后通过 Electron 主进程原生拉起 PaddleOCR-json 进行超低延迟、超高精确度的图片流解析工作。
    - **修复方案**:
        1. 清理 `src/App.tsx` 所有的 `GoogleGenAI` 与 `Tesseract` 远程和本地库引用。
        2. 清理 `EntryModalContent` (输入弹窗) 和 `standaloneMode` (独立挂件) 组件所有的 OCR loading 状态、拖曳拖放加载、文件粘贴、选择图片等 DOM 组件与事件绑定。
        3. 将其回归为纯文本智能秒回显输入流结构，确保前端打包文件极速加载编译，并为下一步桌面应用使用 Electron 管道直接对 PaddleOCR-json 执行进程通信提供了完美的干净土壤。
    - **AI提示词**: 清除 `src/App.tsx` 头部 `Tesseract` 与 `GoogleGenAI` 插件引入和挂载逻辑。移除 EntryModalContent 和独立挂件页面所有的 OCR Mode 滤镜、文字图片粘贴及拖放识别等重度业务节点，保持轻量无污染的文字流交互界面，为本地 PaddleOCR系统集成做最终的架构净化。
    - **问题现象**: 
        1. 由于历史上系统的计算或撤销逻辑包含强行将数值用 `Math.max(0, ...)` 截断清零的操作。
        2. 若之前存在累加或减法撤销时的时序差额，强行截断成零会导致底层的精确数值还原失效。比如某次产生对等减法回卷时，由于被强行置零，在下一次撤销时便会导致号码矩阵多进、多余添加账目，逻辑发生了 Regression 不对称。
    - **修复方案**: 
        1. 按照用户最新指令，在 `src/utils/lotteryParser.ts` 中**彻底不支持、不识别**任何形式的负数输入及扣减解析（如 `01各-1000` 或 `01减1000` 均当作非法数据过滤或按正息处理，防止误操作入账）。
        2. 在 `src/App.tsx` 中移除入账与撤销（`handleUndo`）中针对 `financeBetData` 底层操作的 `Math.max(0, ...)` 物理截断，使其始终保持精密的代数可逆加减。
        3. 号码分布矩阵 `NumberMatrix` 数据展示扩展至 `amount !== 0`，如果外部调账或历史记录合并意外出现了负数（小于零的残值），UI 字体标记为红色（`text-red-500`）并带有柔和浅红背景高亮（`bg-red-50/70 border-red-200`），直观警示异常状况，其余全生命周期的撤销运算可以完美在加与减之间对等抵消还原。
    - **AI提示词**: 排除金额负值解析支持，保持单纯正数流水。移除录入和 `handleUndo` 等修改账面函数内的 `Math.max(0, ...)` 物理阻断，保持精密 1:1 回滚并支持矩阵极端负值残值红字醒亮警示。
    - **问题现象**: 点击“吃码上报”后，系统在汇总模式下会对 `summaryMatrixData` 做扣算削减（`displayBetData - eatenAmounts`），导致主界面的“号码分布矩阵”以及成交额统计大幅变小，不符合账目保留原状的财务审计习惯。
    - **修复方案**: 
        1. 将 `summaryMatrixData` 更改为始终返回完整的 `displayBetData`，从而保持号码分布矩阵和成交额原封不动，彻底阻断任何去重上报对基础下注账目的削减干扰。
        2. `riskDisplayData` 与 `RiskAnalysisRanking` (风险排名与统计列表) 仍然引入 `eatenAmounts`，使得实地算式与吃码解算能敏锐捕获到吃码上报数据并随动计算。
    - **AI提示词**: 号码分布矩阵数据源 `summaryMatrixData` 必须与已上报状态 `eatenAmounts` 解耦，确保吃码上报的结果仅对风险数区块及解算器生效，不得导致号码分布矩阵发生数值扣减。

- [2026-05-19] 归一化“各包”平摊逻辑分拆
    - **问题现象**: 用户输入“狗猴各包100元”时，系统将其算作整体平摊（8个号分摊100元，每个号12.5元），然而用户的真实意图为“狗包100 猴包100”（即狗的4个号瓜分100，猴的4个号瓜分100，每个号25元）。
    - **修复方案**: 
        1. 在 `parseSegment` 判定 `isSplitAmount` (即 keyword 为 "包") 且该分段文本包含 "各" 或 "各包"（`hasEachSplitting`）时，对内部产生的 `parsedGroups` 进行逐一分拆。
        2. 将每个目标群组作为独立的 ParseResult 结果项返回（其金额按照该特定小组成员数量平摊，`amount / group.numbers.length`），从而完美遵循各目标单独平摊的语义。
    - **AI提示词**: 当“包”与“各”共同出现执行分账平摊逻辑时，检测 `hasEachSplitting` 并在有多个群组的情况下执行 `map` 动作拆解成多项 `ParseResult` 分别计算并返回，以实现独立平摊。

- [2026-05-19] 归一化错别字识别：名 = 各
    - **问题现象**: 用户在输入下注指令时，由于输入习惯或打字手误，有时会把“各”（如“各20”）错打成“名”（如“名20”），例如“红双名15”或“龙虎名50”，系统由于无法匹配该关键字导致产生丢单。
    - **修复方案**: 
        1. 在 `src/utils/lotteryParser.ts` 内部的 `TYPO_MAP` 纠错表中加入 `'名': '各'`，从源头确保任何进入系统的“名”都会在预处理最开始阶段翻译为标准赋值字符“各”。
        2. 彻底无缝融合标准金额运算机制，防止业务遗漏。
    - **AI提示词**: 系统处理输入文本时，必须在最早的纠错/归一化（Typos Correction）阶段将“名”转换为标准的“各”，再进行金额提取与分割。

- [2026-05-20] 解决复制/自动/手动导入文本写入输入框时跳过翻译净化的问题
    - **问题现象**: 
        离线 OCR 识别完成后调用 `finalCleanText` 是有效的，但当用户走“剪贴板自动同步抓取”、“聚焦自动读取剪贴板”、“清空并粘贴（triggerClearAndPaste）”以及手动点击“粘贴识别（handlePasteAndRecognize）”等通道粘贴了可能存在形似错漏字（如“午”、“件”、“吗”）的网络文本时，系统是直接原封不动填充到输入框 `modalInputValue` 的，绕过了 `finalCleanText` 字典重译清洗，导致后续语法高亮及解析识别失败，甚至用户需要手动二次修改。
    - **修复方案**:
        1. 对 `App.tsx` 中的所有外部文本导入端点（包括 `clipboard-data` 事件接收、窗口 Focus 剪贴板监听、`triggerClearAndPaste` 大面积重置、`handlePasteAndRecognize` 粘贴识别）实施拦截。
        2. 全量在前置写入状态 `setModalInputValue` 和 `setLocalModalValue` 的那一瞬间，先跑一次 `finalCleanText(text)`，使输入框呈现出 100% 格式化、标点归一、错别字消解的高级纯净指令集。
    - **AI提示词**: 前端所有的剪贴板获取/粘贴机制在更新输入框状态（`setModalInputValue`）前，必须一律物理执行 `finalCleanText` 跑一次纠偏字典。

- [2026-05-20] 图片识别与复制文本中的符号统一清洗为点号，英文干扰字符统一擦除
    - **问题现象**:
        由于手写字或拍照 OCR 的不确定因素，会生成大量不符合彩票大单格式的杂碎英文字符干扰（如 a, b, x, xcr 等）及不同的手写连笔标点（如逗号、减号、反斜杠、下划线、顿号、冒号）。原有的 `finalCleanText` 使用冒号 `：` 部分归一，但这会在输入中产生不必要的手工感觉、而且由于对点号处理繁冗，限制了列表隔离提取器的流畅执行。
    - **修复方案**:
        1. 重新设定 `finalCleanText` 物理层机制。在开始时直接使用正则表达式 `/[a-zA-Z]/g` 将所有无语义英文字符强制擦除，净化数据底色。
        2. 将原有的多重非文字和标点转换（包括中英文点号、中英文逗号、顿号、减号、冒号、下划线 `[\.\,，、\-:_：]`）全部强行替换并清洗为标准分隔点号 `.`。
        3. 对可能由于替换和原文冲突形成的多个连续点号进行化简，通过 `replace(/\.+/g, '.')` 统一折叠为单个点号。
    - **AI提示词**: OCR 统一和复制粘贴清洗：在 `finalCleanText` 阶段最前部使用 `replace(/[a-zA-Z]/g, '')` 物理抹去多余英文字符，随后使用 `replace(/[\.\,，、\-:_：]/g, '.')` 将一切杂牌顿号、逗号、滑号和冒号替换为干净的 `.` 符号，最后用 `/\.+/g` 折叠成单独一星点作为号码及指令分界。

- [2026-05-20] 修复 "01 234尾"等模式下 01 被错误展开为尾数的问题
    - **问题现象**: `expandHeadTail` 将 `01` 识别为单位数并执行了尾数展开（由于之前的 `isSingleDigit` 包含了 `startsWith('0')` 逻辑），导致 `01 234尾` 被错误识别为 `0尾 1尾 2尾 3尾 4尾`。
    - **修复方案**: 修正 `isSingleDigit` 的定义，严格限定 `part.length === 1` 为单位数。这使得 `01` 被视为双位独立球号，而 `234` 在紧贴后缀时仍能正确展开。
    - **AI提示词**: `expandHeadTail` 中的单位数判断必须严格遵循 `length === 1`，严禁将补零后的双位数（如 `01`）视为可展开的单位数。

- [2026-05-20] 在主进程 OCR 与彩票统计软件中添加终极业务纠错字典 (TRADING_DICTIONARY & finalCleanText)
    - **问题现象**: 
        下注人利用手写或拍照发图在离线 OCR 识别时，部分玩法字极其容易被识别为相似错别字。由于本地 PaddleOCR 离线底层字典无法直接改写，当识别出相似字（如 “吗/吗” 错字、“午” 代替 “牛”、“件” 代替 “牛”、“和单” 代替 “合单”、“兰波” 代替 “蓝波” 甚至把连接符和冒号错认成其它符号）时，系统的语法解析和流水分块将发生错杂。
    - **修复方案**:
        1. 在 `src/utils/lotteryParser.ts` 引入终极替换业务纠错字典 `TRADING_DICTIONARY`，合并容错常见错字：生肖（鼠、牛、虎、兔、龙、蛇、马、羊、猴、鸡、狗、猪）与玩法（红蓝绿波、合单、合双、港、澳）。
        2. 实现 `finalCleanText` 强力清洗函数：在进行任何文本解析填入前，对识别出的拼音或错字字符串执行快速翻译重组，将错字以及手写点号、顿号等统一转换为 `：`，帮助后续正则切分。
        3. 同样在解析核心的预处理 `correctTypos` 入口处结合 `TRADING_DICTIONARY` 映射机制，使手动直连输入及复制粘贴输入时亦能秒级自愈。
        4. 在 WebApp (App.tsx) 拿到 OCR `res.text` 之后将其首先导入 `finalCleanText(res.text)` 转换清洗，然后再更新输入框，实现一键录入高鲁棒性对账。
    - **AI提示词**: 在 `src/utils/lotteryParser.ts` 生存统一 `TRADING_DICTIONARY` 并提供 `finalCleanText` 用于过滤和汉字形似字强修，且在 `App.tsx` 每次拉起 OCR 极速解析完文本准备录入时优先执行 `finalCleanText(res.text)`，再传输清洗后的规范字符串进输入框中。

- [2026-05-14] 修复“头/尾/范围/分类”在并集模式（空格分隔）下被忽略的问题
    - **问题现象**: 引入空格敏感的 `wordSegments` 逻辑后，原有的全局正则提取逻辑（head/tail/range）产出的结果未被正确合并到最终的分段对象中，导致“9尾 10各20”只能识别到 10。
    - **修复方案**: 将“头/尾/范围/合数/分类”的探测逻辑全面移入 `wordSegments` 循环内部，确保每个单词均经过动态模式扫描，并在交集（intersection）和并集（union）两条路径中均提供支持。
    - **AI提示词**: 在 `parseSegment` 的单词循环中，必须优先执行动态模式（Head, Tail, Range, Composite, Category）的匹配，并将结果同步注入 `intersectionFilters`（用于交集检测）和 `localTokens`（用于并集并保留顺序）。

- [2026-05-14] 规范波色显示逻辑
    - **问题现象**: 用户输入“红双”等交集组合时，预览中“红”字后的“波”后缀失踪。
    - **修复方案**: 在 `parseSegment` 最后拼装 `raw` 时，对 `rawTokens` 进行检查，若存在孤立的“红/蓝/绿”关键词，强制补齐为“红波/蓝波/绿波”。
    - **AI提示词**: 规范化显示：在处理并集或交集目标时，针对单一颜色词强制补齐“波”后缀。

- [2026-05-14] 增强交集/并集空格敏感识别及波色自动展开
    - **问题现象**: 
        1. 之前逻辑对交集的判定在空格处理上不一致。用户要求：**空格是核心分界线**：有空格为并集（如“红双 小”表示“红双”和“小”各多少），无空格为交集（如“红双小”表示满足三种属性的号码）。
        2. 复合波色（如“蓝绿波”）识别后显示不完整，且丢失了“波”字后缀（变为“蓝 绿”）。
    - **修复方案**: 
        1. 重构 `parseSegment`：首先按空格将片段切分为单词。对**每个单词内部**执行交集探测；如果单词内部有多个属性且交集有效，则该单词视为一个交集下注。
        2. 单词与单词之间、单词与独立数字/生肖之间统一视为并集，预览时强制使用空格分隔（`rawTokens.join(' ')`）。
        3. 引入 `expandWaves` 预处理函数：将“蓝绿波”等模式识别并替换为“蓝波 绿波”，确保下游逻辑能正确提取到完整的“颜色+波”属性。
    - **AI提示词**: 
        1. **规范颜色显示**：在 `parseSegment` 的最后拼接阶段，对 `rawTokens` 进行检查，若存在孤立的“红”、“蓝”、“绿”，必须统一追加“波”后缀。
        2. **禁用波字剥离**：在 `cleanDisplayRaw` 中不再强制剥离“波”字。
        3. **并集不计算去重**：针对并集场景（空格分隔），号码总数应为各组数量之和，不执行 Set 去重。
11: 
12: - [2026-05-13] 修复风险分析排名未跟随“风险容忍限额 (eatingThreshold)”随动的问题
    - **问题现象**: 风险分析排行榜（RiskAnalysisRanking）在“统计”和“吃码”页面中，其红色高亮背景仅基于 `risk < 0` 进行硬编码判断，未考虑用户设定的风险容忍度。
    - **修复方案**: 为 `RiskAnalysisRanking` 组件增加 `threshold` 属性，并将其逻辑修改为 `risk < -threshold`。同时同步更新所有调用处，确保全局风险预警与用户设置一致。
    - **AI提示词**: 风险分析高亮必须动态引用 `eatingThreshold` 状态。对比逻辑为 `risk < -threshold`，不得硬编码为 0。

- [2026-05-13] 增强组合类型识别 (大单/大双/小单/小双) 及 "包" 金额平摊逻辑
    - **问题现象**: 1. 用户输入“大单”时，系统将其拆分为“大”和“单”的并集，而非交集；2. 用户需要一种方式平摊总金额到多个号码上（如“龙包100”）；3. “小数双”等变体未能正确识别为“小双”。
    - **修复方案**: 
        1. 在 `combinations` 数组中显式增加 “大单”、“大双”、“小单”、“小双” 以及 “合单单”、“合双双”、“合大大”、“合小小” 组合，确保其在单项词（如“大”、“单”）之前被优先匹配并执行交集过滤。
        2. 将 “包” 加入 `STRONG_KEYWORDS`，并在 `parseSegment` 逻辑中识别该关键字。若匹配到 “包”，则将提取到的金额除以目标号码总数，实现平摊。
        3. 在 `parseSegment` 和 `cleanDisplayRaw` 中增加“大数/小数/单数/双数”到“大/小/单/双”的归一化，并修正交叉组合（intersection）逻辑，使其基于归一化后的字符串进行匹配。
    - **AI提示词**: “大单”等组合词必须在 `combinations` 中显式定义为交集过滤器。新增 “包” 关键字逻辑。在提取逻辑中，优先执行“大数”等词汇的归一化，且交叉识别（Intersection Filters）必须基于归一化后的 `remainingStr`。

- [2026-05-15] 修复“清空数据并粘贴”按钮在独立打包/Electron环境下的聚焦丢失问题及清空逻辑
    - **问题现象**: 1. 在录入助手点击“清空数据并粘贴”并确认后，虽然数据已载入，但输入框（textarea）失去焦点且点击无法产生光标；2. 清空逻辑不彻底，可能导致旧数据残留。
    - **修复方案**: 
        1. **强制重绘 (textareaKey)**: 引入 `textareaKey` 状态。在触发清空或粘贴操作时递增该 Key，强制 React 销毁并重新挂载 `textarea` DOM 节点，以此重置其内部的焦点状态和渲染层。
        2. **独立 Ref 分离**: 为独立窗口（Standalone）显式使用 `standaloneInputRef` 替代主窗口的 `modalInputRef`，防止 Ref 冲突。
        3. **多级聚焦保障**: 在 `confirm` 阻塞结束后，依次执行 `document.activeElement.blur()`（失焦清理） -> `window.focus()`（窗口置顶显效） -> `input.focus()`（元素聚焦）。
        4. **输入框交互补强**: 
            - 显式设置 `caretColor: '#141414'`（黑光标），并增加 `spellCheck={false}` 减少系统干扰。
            - 在 `onPointerDown` 和 `onClick` 事件增加 `e.stopPropagation()` 及强聚焦请求。
        5. **核心清空逻辑 (Master Wipe)**: 
            - **物理爆破**: 遍历 `localStorage` 匹配所有业务键（`customer_state_`、`financeRecords` 等），将其物理重置为初始状态值（如 `emptyBet` 或 `[]`）。
            - **内存熔断**: 发送 `LOTTERY_RESET_REQUEST` 全局信号（含港/澳前缀），强制所有窗口同步执行 `handleReset` 抹除当前的内存状态，防止切换时旧数据回流。
    - **AI提示词**: 修改“清空数据并粘贴”逻辑时，必须通过 `setTextareaKey` 触发重绘，并配合 `window.focus()` 使用至少 500ms 延迟的异步聚焦。清空操作必须涵盖物理爆破（localStorage）和内存熔断（global signal）两步。

- [2026-05-12] 修复“清空数据并粘贴”按钮失效及跨窗口/Electron同步问题
    - **问题现象**: 1. 点击“清空数据并粘贴”后，输入框未更新内容或未触发表单重置；2. 在独立录入窗口下，清空信号无法同步到主窗口；3. 粘贴后的文本未同步更新 `localModalValue` 导致预览区域显示滞后。
    - **修复方案**: 
        1. 在 `triggerClearAndPaste` 中显式读取剪贴板并发起 `handleReset` 同步。
        2. 引入 `LOTTERY_RESET_REQUEST` 全局信号（以及 Electron 的 `reset-entry`），确保跨进程数据清空。
        3. 将 `triggerClearAndPaste` 及其依赖函数接入 `useMemo` 的依赖项，确保子组件引用的是最新闭包。
    - **AI提示词**: “清空数据并粘贴”必须同时更新 `modalInputValue` 和 `localModalValue`。在 `standaloneMode` 下必须通过 `localStorage` 或 `electron.send` 发送全局重置信号。

- [2026-05-12] 扩展关键词归一化 (个 -> 各)
    - **问题现象**: 用户习惯使用“个”作为金额赋值符（如“10个20”），系统有时无法稳定识别为“各”。
    - **修复方案**: 在 `TYPO_MAP` 中将 `'个'` 映射为 `'各'`。
    - **AI提示词**: 将金额语义词 `'个'` 加入解析前的 `TYPO_MAP` 归一化列表，确保其逻辑与 `各` 保持一致。

- [2026-05-09] 修正尾数展开抑制逻辑。解决“14257尾”在带点号列表环境下被错误抑制并剥离的问题。
    - **问题现象**: 输入 `11. 14257尾` 时，解析器将 `14257` 视为金额或普通号码，而非尾数展开。
    - **修复方案**: 在 `expandHeadTail` 中加入 `val <= 49` 判定。只有当疑似尾数的数字属于合法球号范围时，才在列表环境中执行抑制逻辑。
    - **AI提示词**: 修改 `expandHeadTail` 中的 `inhibitExpansion` 判定，增加 `val <= 49` 条件。

- [2026-05-11] 修正汇总页面缩放逻辑失效及显示不一致
    - **问题现象**: 1. 汇总页面的号码分布矩阵错误显示了原始总额而非按系数缩放后的金额；2. 吃码上报解算器及风险排名在汇总模式下未考虑各客户的算式系数。
    - **修复方案**: 
        1. 引入全局 `displayBetData` memo。当 `selectedCustomerId === 'default'` 时，强制返回 `scaledBetData`。
        2. 将 `totalTurnover`、`NumberMatrix`、`previewReportedData` (解算器) 以及 `riskDisplayData` 统一重构为使用 `displayBetData`。
    - **AI提示词**: 汇总页面（Summary Mode）的所有视图组件和逻辑计算必须统一依赖于 `displayBetData` (即 `scaledBetData` 的别名)，确保系数缩放逻辑贯穿全站。

- [2026-05-11] 录入弹窗客户状态持久化优化
    - **问题现象**: 录入弹窗再次打开时，默认总是选中第一个客户，用户希望保持上次录入时的选择状态。
    - **修复方案**: 
        1. 在 `popOutTargetId` 的初始化逻辑中加入从 `localStorage` 读取 `last_recorded_customer_id` 的判定。
        2. 在 `handleParse` 成功保存数据后，将 `popOutTargetId` 物理记入 `last_recorded_customer_id`。
    - **AI提示词**: 实现 `popOutTargetId` 时，必须结合 `localStorage` 记录 `last_recorded_customer_id`。保存下注时同步更新该 ID，确保弹窗再次打开时能恢复上一次的上下文。

- [2026-05-11] 吃码上报页面 UI 逻辑重构 (双框显示)
    - **问题现象**: 用户希望更直观地看到汇总额减去上报额的过程，并增加“汇总数”和“余下上报”的标题。
    - **修复方案**: 
        1. 在吃码矩阵上方增加每列对应的“汇总数”与“余下上报”标题。
        2. 将单一数值框拆分为双框：左框显示 `总额 - 已上报` (实地自留)，右框显示 `本次建议上报差额`。
        3. 利用 React 自动响应 state 变化的特性，使执行上报后数值自动从右框“流动”到左框的扣减逻辑中。
    - **AI提示词**: 吃码上报页面必须采用双框布局（汇总数 vs 余下上报），且左框始终展示扣除已上报后的实地余额。确保执行操作后，界面能实时反映“金额从右框转移并导致左框进一步扣减”的业务流向。

- [2026-05-11] 智能系统识别开关与默认系统调整
    - **问题现象**: 用户希望可以关闭自动识别“港/澳”地区词的功能，关闭后所有不带地区词的录入应强制归属澳门系统；同时“精准撤销”应默认关闭。
    - **修复方案**: 
        1. 在设置中新增 `smartSystemRecognition` 开关（默认 ON）。
        2. 将 `enableSearchUndo` 的默认初始值改为 `false`。
        3. 修改 `splitBySystem`：当 `smartSystemRecognition` 为 OFF 时，忽略所有关键词，直接返回单一的澳门（MO）下注片段。
    - **AI提示词**: 系统设置新增 `smartSystemRecognition` 开关。`splitBySystem` 必须首选校验此标志，若为 OFF 则强制返回 `[{ system: 'MO', text: input }]` 且不进行关键词分割。精准撤销默认关闭。

- [2026-05-11] 修正港澳特码系统识别与中奖判定错位
    - **问题现象**: 1. 香港系统的下注在结算时错误地使用了澳门特码框的数值进行判定（反之亦然），导致“指东打西”的中奖错误。
    - **修复方案**: 
        1. 修正历史流水显示（sidebar/modal）以及报表导出中的中奖判定逻辑，显式通过 `item.system`（若有）或 `record.system` 结合系统前缀来动态选择对应的特码框数值（`auxSpecialNumber` vs `specialNumber`）。
    - **AI提示词**: 所有涉及中奖判定的代码必须基于 `item.system` 动态选择 `hkSpecial` (aux) 或 `moSpecial` (main) 框的值，严禁硬编码单一系统判断。无引导词录入默认归属 MO。

- [2026-05-11] 报表批注框自适应尺寸优化
    - **问题现象**: 导出的 Excel 报表中，下注金额列的批注（Annotations）框尺寸固定且往往过小，导致长文本原数据被遮挡。
    - **修复方案**: 在导出逻辑中引入 `wpx` 和 `hpx` 计算逻辑。根据 `originalText` 的行数以及最长行的字符跨度（区分中英文权重），动态设置批注框的宽（wpx）和高（hpx）。
    - **AI提示词**: 在 Excel 导出时，通过 `d.originalText` 动态计算 comment 对象的 `p: { wpx, hpx }` 属性，确保批注内容完整显示。

- [2026-05-11] 解决清空面板持久化失效及跨系统不同步问题
    - **问题现象**: 1. 清空面板后切换系统会还原数据；2. 清空某一系统（如香港）的面板时，同一客户在另一系统（如澳门）的数据依然保留，未达到“一键清空该客户”的效果。
    - **修复方案**: 
        1. 在 `handleClearBoard` 中显式循环 `['HK_', 'MO_']` 前缀。
        2. 同步执行 `localStorage.setItem` 对两个系统的该客户状态（`customer_state_${id}`）进行物理覆盖。
    - **AI提示词**: “清空面板”逻辑必须包含同步的 `localStorage.setItem` 物理写入，且必须**遍历所有前缀 ('HK_' 和 'MO_')** 来同时清除该客户在港、澳两个系统的业务数据。

- [2026-05-10] 移除所有录入目标的强制去重
    - **问题现象**: 输入 `40 40各15` 或 `0*1*9*2*9尾各30` 时，解析器自动去重，导致重复的号码或尾数只录入了一次。
    - **修复方案**: 
        1. 移除 `normalizeComposites` 逻辑中的 `seenAggregates` 去重 Set，允许所有目标（号码、分类、头尾等）在片段内重复出现。
        2. 确保 `parseSegment` 返回结果前的 `numbers` 组装逻辑不执行 `Set` 去重，允许重复项透传。
    - **AI提示词**: 解析逻辑应尊重用户手动输入的重复意图，严禁对任何目标类别执行自动去重。在 `normalizeComposites` 和 `parseSegment` 结尾处均不得使用 `Set` 或其它逻辑过滤重复项。

- [2026-05-10] 扩展金额关键词归一化
    - **问题现象**: 类似 `一个号`、`各号` 等词汇在某些复杂输入下无法被识别为金额赋值符。
    - **修复方案**: 将 `一个号`、`各号`、`每个号`、`个号` 加入 `TYPO_MAP` 统一归一化为 `各`。
    - **AI提示词**: 将金额语义词（如 `一个号`、`各号` 等）加入解析前的 `TYPO_MAP` 归一化列表，确保其逻辑与 `各` 保持一致。

- ## 颜色类
- 兰 -> 蓝
- 绿 -> 绿 (注：防止部分输入法输出繁体或异体字)
- 红 -> 红

## 系统/地区类
- 门 -> 澳 (在特定上下文，但在系统识别中已有逻辑处理)
- 港码 -> 港
- 澳码 -> 澳

## 生肖类
- 猪 -> 猪
- 鼠 -> 鼠
- 虎 -> 虎

## 数字类
- 零 -> 0
- 一 -> 1
- 二/两 -> 2
- 三 -> 3
- 四 -> 4
- 五 -> 5
- 六 -> 6
- 七 -> 7
- 八 -> 8
- 九 -> 9
- 十 -> 10

## 逻辑类
- 一字 -> 各
- 字 -> 各
- 个字 -> 各
- 个 -> 各
- 一粒 -> 各
- 俩 -> 2

## 逻辑追溯记录
- [2026-05-10] 合单/双/大/小 识别逻辑修正 (深度优化：动态交集 & 紧凑显示)
    - **问题现象**: 1. 硬编码复合词导致识别逻辑判定失效；2. `cleanDisplayRaw` 曾因 `complexRegex` 不完善而向 `合单单` 等复合词注入空格，导致其被误识别为并集而非交集。
    - **修复方案**: 
        1. 依赖 `intersectionFilters` 动态拆分识别。
        2. 增强 `complexRegex` 支持 `[大小单双]+`，并在 `normalizeComposites` 中有条件地保留“合”字（仅在紧跟数字时补“数”）。
        3. 彻底修复 `cleanDisplayRaw` 逻辑，确保 `合单单` 等在预览中保持紧密不分离 (raw: "合单单")，从而触发正确的无分隔符逻辑。
    - **AI提示词**: 实现复合属性识别时，必须确保 `cleanDisplayRaw` 不破坏输入词的紧凑性。复合正则匹配必须支持贪婪匹配 `[特性词]+`。

- [2026-05-10] 合数识别与显示优化 (二次修正)
    - **问题现象**: `合数11` 被错误显示为 `合数1 01`，且某些情况下出现 `合数12 合数 12` 这种重复显示。
    - **修复方案**: 
        1. 引入统一的 `normalizeComposites` 函数，在解析（parseSegment）和预览（cleanDisplayRaw）前执行。
        2. 确保合数及其数字在 `targetsStr` 中被完全归一化并通过 `complexRegex` 实施保护，防止被 `cleanRawText` 错误分割。
        3. 移除 `cleanDisplayRaw` 中相互重叠的逻辑，确保 expansion 逻辑幂等。
    - **AI提示词**: 处理合数显示和解析时，必须在提取独立数字前调用统一的 `normalizeComposites`。该函数应确保“合X”与“X合”均被规整为“合数X”，防止 `cleanRawText` 将多位数（如11）拆分。

- [2026-05-10] 合数识别逻辑修正
    - **问题现象**: `合数10` 等多位数被拆分，且对 `合10` 的定义不明确（应为绝对和）。
    - **修复方案**: 
        1. 修改 `cleanDisplayRaw` 中的 regex，使用 `\d+` 匹配数字块，保留 `10`, `11` 等整体，并确保预览中不带空格。
        2. 合数计算逻辑优化：如果值 `val >= 10`，执行绝对和匹配 `(tens + ones) === val`；如果 `val < 10`，执行尾数和匹配 `(tens + ones) % 10 === val`。
        3. 解析后对号码集合执行 `Set` 去重。
    - **AI提示词**: 修改合数解析逻辑时，必须使用 `\d+` 匹配数字块。计算时：值 >= 10 则匹配 `(tens + ones) === val`，值 < 10 则匹配 `(tens + ones) % 10 === val`。

- [2026-05-10] 增加“俩”字映射。
    - **问题现象**: 用户习惯直呼“俩”表示数字 2（如“俩尾”）。
    - **修复方案**: 在 `TYPO_MAP` 中将 `'俩'` 映射为 `'2'`。
    - **AI提示词**: 语义归一化应包含 `'俩': '2'`。

- [2026-05-10] 修复录入助手撤销/清空功能失效。
    - **问题现象**: 独立窗口模式下点击“撤销”或“清空数据并粘贴”无反应。
    - **修复方案**: 统一跨窗口信号键名。发送端在 `localStorage.setItem` 时增加 `getSysKey` 前缀，使其与接收端保持一致。
    - **AI提示词**: 任何跨窗口通过 `localStorage` 传递的控制信号（如 `UNDO`、`RESET`）必须通过 `getSysKey` 包装，以适配港澳系统隔离。

- [2026-05-10] 修正 `6尾各50. 3. 9尾` 场景下的识别失败。
    - **问题现象**: `.` 紧跟在金额 `50` 后时，导致 `expandHeadTail` 将其连同后面的 `3` 错误捕获，且由于 `50` 存在，触发了“多位数抑制”，导致 `3` 无法展开为 `3尾`。
    - **修复方案**: 
        1. 在 `preProcessed` 中将 `金额. `（点号加空格）规范化为 `金额 `。
        2. 修正 `expandHeadTail` 的抑制逻辑，明确规定只有 `1-49` 之间的数字才具备抑制周边号码展开的能力。
    - **AI提示词**: 任何 > 49 的数字都不应被 `expandHeadTail` 的 `inhibitExpansion` 逻辑识别为“多位球号干扰项”。

- [2026-05-10] 修复跨行头尾展开干扰及非法 00 显示。
    - **问题现象**: 输入多行数据时，第二行的单数字（如 1.0.5...）未能按“尾数”展开，反而显示为 `00`。
    - **修复方案**: 
        1. 在 `expandHeadTail` 正则中将 `\s` 替换为 `[ \t]` 以物理隔离换行符。
        2. 在 `cleanDisplayRaw` 和 `smartSplitDigits` 中增加范围校验，拦截独立数字 `0`。
    - **AI提示词**: `expandHeadTail` 的匹配缓冲区严禁包含 `\n`。在 `cleanDisplayRaw` 格式化预览时，必须校验 `val >= 1` 才执行补零。

- [2026-05-10] 修正流水记录展示顺序。
    - **问题现象**: 入账流水的片段顺序与识别助手中的顺序不一致（受金额排序影响）。
    - **修复方案**: 移除流水生成时的金额排序逻辑，改用 `items.map` 直接按识别顺序生成预览。
    - **AI提示词**: 保持 `segmentPreview` 的生成逻辑与 `items` 的原始顺序一致，严禁引入 `sort` 或基于金额的 `Record` 聚合。

- [2026-05-10] 强化“一字认做各”逻辑。
    - **问题现象**: 用户习惯使用“一字”或单纯的“字”作为金额赋值符，若未显式处理可能导致解析失败或显示混乱。
    - **修复方案**: 在 `TYPO_MAP` 中加入全局映射，将相关词汇统一归正为“各”。
    - **AI提示词**: 在 `correctTypos` 或 `TYPO_MAP` 中加入 `'一字': '各'`, `'字': '各'` 等映射，实现语义归一化。

- [2026-05-09] 建立纠错机制。解决“兰”等错别字导致颜色识别失败的问题。
    - **问题现象**: 用户输入“兰波”或“兰尾”，系统无法识别颜色。
    - **修复方案**: 在解析前执行全局字符替换。
    - **AI提示词**: 在解析逻辑开始前，调用 `correctTypos` 函数执行预定义的正则替换。

- [2026-05-20] 优化平摊玩法（包字）存入号码矩阵与中金中奖金额的计算，舍弃四舍五入全部归一为向下取整
    - **问题现象**: 
        1. 当录入平摊玩法（如“鼠包10”，包含 4 个号码，每个号码平摊 2.5 元）时，若按照原逻辑直接将浮点数 2.5 直接或半四舍五入的形式存入矩阵对账会造成混乱。用户明确指出当使用“包”字平摊玩法录入时，写入号码分布矩阵的数据应该直接 **取整（向下取整，即鼠各2）**；
        2. 原本的提示后缀 `'（四舍五入）'` 应当同步改正为代表全新向下取整行为的 `'（向下取整）'`；
        3. 表格/流水/导出 Excel 中的中奖金额列（中金），原逻辑在平摊非整数值时会产生四舍五入的浮点差错。如果是平摊玩法的号码中奖，该条平摊玩法中奖金额应当统一 **向下取整（Floor）**，而非四舍五入。
    - **修复方案**:
        1. 在下注保存与对称撤销（`handleUndo`）逻辑写入 / 移除 `financeBetData` 时，当属性 `isSplitAmount` 为真（或称作平摊玩法）时，使用 `Math.floor(it.amount)` 与 `Math.floor(item.amount)` 将其实际下注金额向下取整后，再加减合并入矩阵；
        2. 全面将 App 录入助手和账目历史预览在 `isSplitAmount` 为真时代表计算策略的后缀字符串 `'（四舍五入）'` 修改换为 `'（向下取整）'`；
        3. 重新制定中奖金额（中金）计算流水方法，无论是记录列表（`winningAmount`）还是 Excel 导出的单码中奖计算，针对平摊模式（`isSplitAmount: true`），在中奖中金项累加乘积前统一使用 `Math.floor(hits * item.amount)` 代替原本的 `hits * item.amount`，消除四舍五入偏离，确保账款对称可对账。
    - **AI提示词**: 当平摊玩法（`isSplitAmount`）生效时，存入号码分布矩阵的数值及对称撤销该条时，必须使用 `Math.floor` 向下取整。同时，在汇总预览及输入助手的合计标志后缀一律翻译并归一化为 `（向下取整）`。针对该类玩法的中奖中金（Winning Amount）计算，无论是列表渲染还是数据导出，必须将分摊金额乘以中奖个数后再进行 `Math.floor` 运算。

